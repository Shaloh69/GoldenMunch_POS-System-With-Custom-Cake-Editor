// Font configuration - use system fonts for build compatibility
export const fontSans = {
  variable: "--font-sans",
  className: "font-sans",
};

export const fontMono = {
  variable: "--font-mono",
  className: "font-mono",
};
